{
  "name": "ai-pirates-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A democratic automation-policy simulator with 1,000 rational, self-interested voters.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "test": "vitest run",
    "preview": "astro preview",
    "deploy": "npm run check && npm test && npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.2",
    "astro": "6.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "6.0.3",
    "vitest": "4.1.10",
    "wrangler": "4.92.0"
  }
}
