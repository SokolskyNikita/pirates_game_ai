{
  "name": "ai-pirates-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An automation-policy simulator with a survey-weighted US electorate and rational, self-interested voters.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run precompute && astro build",
    "check": "astro check && tsc -p worker/tsconfig.json && wrangler types worker/env.d.ts --check",
    "test": "vitest run",
    "preview": "astro preview",
    "deploy": "npm run check && npm test && npm run build && wrangler deploy",
    "types": "wrangler types worker/env.d.ts",
    "precompute": "node scripts/build-precomputed.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.4",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "esbuild": "0.28.2",
    "typescript": "6.0.3",
    "vitest": "4.1.11",
    "wrangler": "4.132.0"
  }
}
